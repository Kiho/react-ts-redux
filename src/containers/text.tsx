import * as React from 'react';
import DemoText from './demo-text';

export default React.createClass({
    render: function () {
        return (
            <div>
                <DemoText />
            </div>
        )
    }
})
